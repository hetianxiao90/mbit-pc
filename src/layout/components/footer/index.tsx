import React from 'react'
import { Layout } from 'antd'

// 全局底部
const LayoutFooter = () => {
  const { Footer } = Layout
  return <Footer className='header'> Footer</Footer>
}
export default LayoutFooter
